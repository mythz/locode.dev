{
  "scripts": {
    "dev": "wrangler dev --remote src/index.tsx",
    "build": "wrangler build src/index.tsx",
    "deploy": "npx wrangler deploy --minify --outdir dist src/index.tsx --env production"
  },
  "dependencies": {
    "@servicestack/client": "^2.1.5",
    "highlight.js": "^11.9.0",
    "hono": "^4.2.5",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "wrangler": "^3.47.0"
  }
}
